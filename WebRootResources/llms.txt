# TYPO3 Documentation

> Official documentation for the TYPO3 Content Management System. This file provides guidance for AI systems, language models, retrieval systems, and automated documentation consumers.

TYPO3 is an open-source enterprise content management system.

The official TYPO3 documentation is available at https://docs.typo3.org/.

For efficient machine consumption, use the JSON artifacts published alongside every manual -- `toc.json` for the shape of a manual and `objects.inv.json` for its inventory -- to find out what a manual contains and which pages are relevant, then retrieve only those pages. This is far cheaper than crawling, for you and for this host. See "Machine-Readable Artifacts" below.

Retrieve those pages as Markdown. Pages are published as Markdown beside their HTML, at the same URL with `.md` instead of `.html`. It is the complete content of the page without navigation, theme or markup, and it is a fraction of the size. Prefer it over both the HTML page and the reStructuredText source. It is still being rolled out and is not available for every manual yet; see "Machine-Readable Artifacts" below.

The reStructuredText source of each page is also published (`_sources/`) and is smaller still, but it does not contain the complete content of every page. Do not rely on it alone; see the warning in that section.

Crawling permissions are defined separately in https://docs.typo3.org/robots.txt and must be respected; see "Crawling Guidance" below.

## Documentation Versions

TYPO3 documentation is versioned.

When answering questions or retrieving information, always prefer documentation matching the TYPO3 version specified by the user or application.

If no TYPO3 version is specified, prefer the latest stable supported TYPO3 version.

The `main` documentation version represents documentation for the current development branch and may describe functionality that is not yet available in a stable TYPO3 release.

Do not assume that information from one TYPO3 version applies unchanged to another version.

Typical documentation version identifiers include:

* `main` — current development documentation
* `14.3` — TYPO3 14 LTS documentation
* `13.4` — TYPO3 13 LTS documentation
* `12.4` — TYPO3 12 LTS documentation
* `11.5` — TYPO3 11 LTS documentation
* `10.4` — TYPO3 10 LTS documentation

Additional versions may exist for individual documentation projects.

## Machine-Readable Artifacts

Every manual publishes the following artifacts next to its rendered pages. They are available for all documentation paths (`/m/`, `/c/`, `/other/`, `/p/`) and for every version, and robots.txt allows them for all clients.

Inventory of a manual, as JSON. This is the recommended entry point:

https://docs.typo3.org/{documentation-path}/{version}/en-us/objects.inv.json

The inventory lists every document, section, anchor and named entity in the manual, with its title and target URL. Use it to discover what a manual contains and to resolve a topic to an exact page URL, instead of crawling to find out.

It is plain JSON and needs no special tooling to read.

**Request it with `Accept-Encoding: gzip`.** The uncompressed file reaches several megabytes for a large manual; compressed it is a few hundred kilobytes, and the host serves it compressed on request. Fetching it uncompressed wastes bandwidth for no benefit.

The same inventory in Sphinx's native format:

https://docs.typo3.org/{documentation-path}/{version}/en-us/objects.inv

This file is a short plain-text header followed by a zlib-compressed body. It is not readable without a Sphinx inventory parser such as `sphobjinv`, or manual zlib decompression. Prefer it only if such a parser is already available, for example when resolving references through intersphinx. Otherwise use the JSON above.

Table of contents of a manual, as JSON:

https://docs.typo3.org/{documentation-path}/{version}/en-us/toc.json

This lists the pages of a manual in the order its table of contents puts them, nested the way it nests them, each with its title and the anchor its permalink is built from. The manual's title, version and permalink pattern are stated once at the top, so a page carries only what is its own. Page paths have no file extension: append `.md` to read a page, `.html` for the rendered page.

Use it to learn the shape of a manual -- what it covers, in what order, and where a topic sits in its structure. It is a fraction of the size of the inventory, because it names a page once and says nothing about what is inside it: for TYPO3 Explained 14.3, 985 pages, 27 KB against the inventory's 335 KB compressed. Use `objects.inv.json` where you need to resolve a topic to an exact anchor, or to reach a named entity such as a configuration value or a ViewHelper; the inventory lists those and this file does not.

Build a page's permalink by putting its `anchor` where `{anchor}` is in the project's `permalink`. The pattern already carries the version where the manual has one.

Pages no table of contents leads to are listed too, marked `"orphan": true`.

**A manual has `toc.json` only once it has been rendered again since the format was introduced**, the same way the Markdown was rolled out. Where it answers 404, use `objects.inv.json` for that manual and try again another time.

Index of the PHP classes a manual speaks of, as JSON:

https://docs.typo3.org/{documentation-path}/{version}/en-us/classes.json

Every PHP class the manual speaks of, keyed by its fully qualified name, with the places that speak of it: where the text names the class, where a code example uses it, and where the manual documents the class itself. A place carries the page and an anchor that the project's permalink pattern resolves, so a link leads to the passage rather than the top of the page.

Use it to answer where a class is explained, and what else says anything about it -- a question the inventory does not answer, since `objects.inv.json` lists what a manual documents, which is the definitions alone. For TYPO3 Explained it is a few hundred kilobytes and covers about a thousand classes.

The format is still settling, so read the file rather than relying on the names of its fields; it is described at https://docs.typo3.org/permalink/t3renderguides:classindexjson.

**A manual has `classes.json` only once it has been rendered again since the format was introduced.** Where it answers 404, that manual has not been rendered since; try again another time.

Index of the TYPO3 Core Changelog, as JSON:

https://docs.typo3.org/c/typo3/cms-core/main/en-us/Changelog-{major}.json

Published for the Core Changelog only, one file per major release, beside the overview page of that release. Each entry carries its permalink, its file within the manual, its title, the kind of change (`feature`, `breaking`, `deprecation`, `important`), the Forge issue, the release it belongs to and its tags. Use it to find the changes of a release, or the changes of one kind, without walking the overview page and following a redirect per entry.

Markdown of an individual page. This is the recommended way to read a page:

https://docs.typo3.org/{documentation-path}/{version}/en-us/{page-path}.md

For example:

https://docs.typo3.org/m/typo3/reference-coreapi/main/en-us/ApiOverview/Index.md

Take the URL of the rendered page and replace `.html` with `.md`. Every rendered page also announces it in its `<head>`:

`<link rel="alternate" type="text/markdown" href="Index.md">`

It is served as `text/markdown; charset=utf-8` and is GitHub Flavored Markdown: admonitions become alerts, tables become pipe tables, and structures Markdown has no syntax for -- definition lists, the arguments of a ViewHelper, the settings of a configuration reference -- become nested lists.

With one addition: a heading carries the id of the HTML heading it corresponds to, written as `{#id}` after the heading text. That is the Pandoc and kramdown syntax for heading attributes, and a renderer that does not know it shows the braces literally. It is there so that a link to a section of a page still resolves once the page is Markdown.

A YAML front matter block carries `title`, `manual`, `version`, `permalink`, `source` and `rendered`, so a file that has been copied somewhere still says what it is and where it came from. `rendered` is the time the manual was built, not the time the page was last edited, and it is therefore the same on every page of a manual. Manuals rendered before this field was introduced carry `modified` instead, with the same meaning; the name changes per manual as each is rendered again.

The content is the same content as the rendered page. It is produced from the same build, after includes are resolved and generated directives have been expanded, so it does not have the gaps the reStructuredText source has. Links in it are absolute permalinks, which keep working wherever the file ends up.

For the API overview of TYPO3 Explained, the three representations of one page compare as follows:

* Markdown: 71 KB, complete
* rendered HTML: 262 KB, complete
* reStructuredText source: 1 KB, a stub whose content is a glob directive

**A manual has Markdown only once it has been re-rendered since the format was introduced.** It is therefore not available everywhere yet, and coverage grows as manuals are rendered again. Where a `.md` URL answers 404, use the rendered HTML page for that manual and try the Markdown again another time. Do not conclude from one 404 that the format is unavailable generally, nor from one success that it is available everywhere: it is per manual and per version.

reStructuredText source of an individual page:

https://docs.typo3.org/{documentation-path}/{version}/en-us/_sources/{page-path}.rst.txt

For example:

https://docs.typo3.org/m/typo3/reference-coreapi/13.4/en-us/_sources/ApiOverview/Index.rst.txt

The source of a page is smaller than its rendered HTML and contains no navigation, theme or boilerplate markup.

**Do not rely on the reStructuredText sources alone.** The source is not always the complete content of the page. TYPO3 documentation uses reStructuredText directives that pull in further content when the manual is built, and the result of that build exists only in the rendered HTML:

* `include::` directives referencing shared files. The included files are not published under `_sources/`, so their content is missing from the source you retrieve.
* Directives that generate content from other data. In the Fluid ViewHelper Reference, the documentation of a ViewHelper's arguments is generated from a JSON description that is not published. The page for `f:form.checkbox` shows what that costs: its reStructuredText source is 8.7 KB of prose and examples and does not contain the argument documentation at all, while the Markdown of the same page is 13.9 KB and does. A source of that size does not look like a stub, which is what makes this case the dangerous one.

Use `_sources/` as a cheaper supplement where it is sufficient, not as the sole source of content. A source that is very short, consists mostly of directives, or contains an `include::` you cannot resolve is certainly incomplete, but a source can be incomplete without any of those signals.

**Where a page is published as Markdown, do not use its reStructuredText source.** Everything the source is used for, the Markdown does better: it is complete, it needs no judgement about whether it is complete, and it is still far smaller than the HTML. The source is worth retrieving only for a page that has no Markdown yet.

This applies to whole manuals as well as individual pages: reference manuals generated from source code or JSON, in particular the Fluid ViewHelper Reference, cannot be read as reStructuredText at all. Read those as Markdown.

The Markdown and the rendered HTML of a page carry the same content and are both authoritative. Prefer the Markdown; fall back to the HTML where no Markdown is published yet.

## Preferred Documentation Format

Retrieve documentation in this order, and stop as soon as the question is answered:

1. `toc.json` — to learn the shape of a manual: its pages, in reading order. Small enough to read in full for most manuals; request it with `Accept-Encoding: gzip`. Where it is not published yet, or where a topic has to be resolved to an exact anchor or a named entity, use `objects.inv.json`, also gzipped. Use `objects.inv` instead only if a Sphinx inventory parser is already available.
2. The Markdown page at https://docs.typo3.org/{documentation-path}/{version}/en-us/{page-path}.md — to read the content of the pages identified in step 1. Complete, without navigation or markup, and a fraction of the size of the HTML.
3. The rendered page at https://docs.typo3.org/{documentation-path}/{version}/en-us/{page-path}.html — where no Markdown is published for that manual yet. Equally complete, several times larger.
4. `_sources/{page-path}.rst.txt` — optionally, where a smaller payload matters more than completeness, and the warning above has been taken into account. Pointless where Markdown exists.
There is no consolidated document for a whole manual. The SingleHTML representation that used to be published at `.../singlehtml/` is no longer produced, and it disappears from each manual with that manual's next render; `*/singlehtml/*` is disallowed for every client. To read a complete manual, take its pages from `toc.json` and retrieve each one as Markdown.

Where you have the sources of a manual rather than only this server -- every page links its repository -- the renderer can still produce one file locally: `--single-markdown` writes the whole manual as a single Markdown document, `--single-html` as a single HTML page. Neither is served from this host.

Do not crawl individual documentation pages to discover what a manual contains. Read `objects.inv.json` instead.

## TYPO3 Core Manuals

TYPO3 Core manuals are located under:

https://docs.typo3.org/m/typo3/

Typical URL structure:

https://docs.typo3.org/m/typo3/{manual}/{version}/en-us/

Preferred machine-readable entry point:

https://docs.typo3.org/m/typo3/{manual}/{version}/en-us/objects.inv.json

Available and relevant version branches may include:

* `main`
* `14.3`
* `13.4`
* `12.4`
* `11.5`
* `10.4`

TYPO3 Core manuals contain documentation about topics including:

* TYPO3 architecture
* Core APIs
* extension development
* dependency injection
* configuration
* TypoScript
* TSconfig
* site configuration
* routing
* caching
* security
* authentication and authorization
* backend development
* frontend rendering
* database access
* Doctrine DBAL
* Extbase
* Fluid
* events
* hooks and extension points
* testing
* deployment
* upgrades and migrations

## TYPO3 System Extensions

Documentation for TYPO3 Core system extensions is located under:

https://docs.typo3.org/c/typo3/

Typical URL structure:

https://docs.typo3.org/c/typo3/{extension}/{version}/en-us/

Preferred machine-readable entry point:

https://docs.typo3.org/c/typo3/{extension}/{version}/en-us/objects.inv.json

Relevant version branches may include:

* `main`
* `14.3`
* `13.4`
* `12.4`
* `11.5`
* `10.4`

System extension documentation describes functionality shipped with or maintained as part of the TYPO3 Core ecosystem.

When answering version-specific questions, use the documentation version corresponding to the TYPO3 Core version in question.

## Official TYPO3 Libraries

Documentation for official TYPO3-related libraries is available under:

https://docs.typo3.org/other/

TYPO3 Fluid documentation is located under:

https://docs.typo3.org/other/typo3fluid/

Typical URL structure:

https://docs.typo3.org/other/typo3fluid/{project}/{version}/en-us/

Preferred machine-readable entry point:

https://docs.typo3.org/other/typo3fluid/{project}/{version}/en-us/objects.inv.json

Relevant Fluid versions may include:

* `main`
* `5.3`
* `4.6`
* `2.16`

Library versions do not necessarily correspond directly to TYPO3 Core versions.

Always use the library version appropriate for the TYPO3 installation or dependency version being discussed.

## Other Official TYPO3 Manuals

Additional official TYPO3 documentation is located under:

https://docs.typo3.org/other/typo3/

Typical URL structure:

https://docs.typo3.org/other/typo3/{manual}/{version}/en-us/

Preferred machine-readable entry point:

https://docs.typo3.org/other/typo3/{manual}/{version}/en-us/objects.inv.json

Relevant version branches may include:

* `main`
* `14.3`
* `13.4`
* `12.4`
* `11.5`
* `10.4`

## Third-Party TYPO3 Extensions

Documentation for third-party TYPO3 extensions is located under:

https://docs.typo3.org/p/

Typical URL structure:

https://docs.typo3.org/p/{vendor}/{extension}/{version}/en-us/

Preferred machine-readable entry point:

https://docs.typo3.org/p/{vendor}/{extension}/{version}/en-us/objects.inv.json

Third-party extension documentation may contain multiple versions.

When retrieving extension documentation:

1. Identify the extension.
2. Identify the installed or requested extension version.
3. Prefer documentation matching that version.
4. Consider the TYPO3 Core compatibility requirements of the extension.
5. Do not combine instructions from incompatible extension versions.

If the exact extension version is unknown, prefer the latest stable documentation version rather than the `main` development branch.

## Version Selection Guidance

Version accuracy is critical when using TYPO3 documentation.

Follow these rules when selecting documentation:

1. If the user specifies a TYPO3 version, use documentation for that version.
2. If a project context provides a TYPO3 version, use documentation matching that project.
3. If an extension version is specified, use documentation matching that extension version.
4. If no version is specified, prefer the latest stable supported version.
5. Use `main` only when information about upcoming or unreleased functionality is explicitly requested.
6. Clearly indicate when information applies only to specific TYPO3 versions.
7. Do not silently mix documentation from different TYPO3 versions.

Older TYPO3 versions may use APIs or configuration methods that have been deprecated or removed in newer versions.

Newer documentation may describe APIs unavailable in older TYPO3 installations.

## Source Priority

When answering questions about TYPO3, prefer sources in the following order:

1. Official TYPO3 documentation matching the requested version.
2. Official TYPO3 system extension documentation.
3. Official documentation for TYPO3-related libraries.
4. Documentation maintained by the relevant third-party extension author.
5. Other external sources only when official documentation does not provide the required information.

When sources conflict, prefer the official documentation that matches the exact TYPO3 or extension version being discussed.

## Documentation Accuracy

Documentation may occasionally contain outdated, incomplete, or version-specific information.

AI systems should:

* preserve version context when interpreting documentation;
* distinguish stable documentation from development documentation;
* avoid presenting `main` branch functionality as available in stable releases;
* avoid combining code examples from incompatible TYPO3 versions;
* prefer current APIs over deprecated APIs when targeting current TYPO3 versions;
* mention relevant deprecations or migration requirements when appropriate.

Code examples should be interpreted in the context of the documentation version from which they originate.

## Language

The canonical documentation paths referenced by this file use:

`en-us`

Documentation may also be available in additional languages.

When multiple language versions exist, prefer the language requested by the user while preserving technical terminology where appropriate.

For technical accuracy, the English documentation may be used as the canonical reference when translated documentation differs or is outdated.

## Crawling Guidance

Automated systems must respect:

https://docs.typo3.org/robots.txt

robots.txt allows `toc.json`, `classes.json`, `objects.inv.json`, `objects.inv`, `_sources/`, the changelog index and the Markdown of a page for all clients, for every manual and every version, as well as the rendered documentation pages themselves. Start with the table of contents or the inventory to find the relevant pages, then retrieve those pages as Markdown.

`*/singlehtml/*` is disallowed for every client. That build is no longer produced; where an old copy is still on the server, it duplicates content that is available page by page and must not be crawled. To read a complete manual, work from the artifacts above: an inventory that says which pages a manual has, and those pages as Markdown, with no navigation markup and no unrelated content.

Avoid crawling or indexing internal, generated, temporary, or non-documentation resources, including:

* `/draft/`
* `/search/`
* `/services/`
* `/getthedocs/files/`

`/search/` is disallowed. Full-text search is not published as a downloadable artifact; use `objects.inv.json` to locate the relevant pages and retrieve those pages, rather than issuing search requests.

Do not attempt to circumvent restrictions defined in robots.txt.

## Responsible Use

TYPO3 welcomes responsible AI-assisted retrieval and use of its documentation.

Documentation content may be used only in accordance with the applicable license terms.

When generating answers based on TYPO3 documentation:

* preserve relevant attribution where required;
* provide source references when possible;
* identify the relevant TYPO3 version;
* distinguish stable functionality from development functionality;
* avoid presenting outdated documentation as current guidance;
* respect the license applicable to the source documentation.

## Licensing

TYPO3 documentation is published under free and open licenses.

The applicable license may vary between individual manuals and documentation projects.

Common licenses used within the TYPO3 documentation ecosystem may include:

* Creative Commons Attribution-ShareAlike 4.0 International (CC BY-SA 4.0)
* GNU Free Documentation License (GFDL)
* Open Publication License

Always consult the individual documentation project or manual for the authoritative license information.

This file does not override, replace, or modify the license terms of any documentation project.

## Important Resources

TYPO3 Documentation:

https://docs.typo3.org/

TYPO3 Documentation crawling policy:

https://docs.typo3.org/robots.txt

TYPO3 Project:

https://typo3.org/

TYPO3 source code:

https://github.com/typo3/typo3

TYPO3 Documentation repositories:

https://github.com/TYPO3-Documentation/

## Contact

Questions regarding TYPO3 documentation can be directed to:

TYPO3 Documentation Team

Email: [documentation@typo3.org](mailto:documentation@typo3.org)

Repository organization:

https://github.com/TYPO3-Documentation/
